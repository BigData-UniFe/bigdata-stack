apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: nifi
  namespace: default
spec:
  hosts:
    - nifi.dev.local
  gateways:
    - default-gateway
  http:
    - route:
        - destination:
            port:
              number: 8080
            host: nifi
